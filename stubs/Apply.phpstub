<?php

namespace FunctionalPHP\FantasyLand;

/**
 * @template T
 * @template-extends Functor<T>
 */
interface Apply extends Functor
{
    /**
     * @template U
     * @param Apply<U> $b
     * @return (T is callable ? Apply<U> : never-return)
     */
    public function ap(Apply $b) : Apply;
}
